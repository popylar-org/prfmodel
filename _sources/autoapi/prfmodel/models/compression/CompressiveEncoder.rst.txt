prfmodel.models.compression.CompressiveEncoder
==============================================

.. py:class:: prfmodel.models.compression.CompressiveEncoder(encoding_model: prfmodel.models.base.BaseStimulusEncoder, min_response: float = 1e-10)



   Compressive encoding model.

   Amplifies and compresses an encoded stimulus response.
   The model has two parameters: `gain` (amplification amplitude) and `n` (compression exponent).

   :param encoding_model: A encoding model instance.
   :type encoding_model: BaseEncoder
   :param min_response: Minimum encoded response (:math:`\epsilon`). A small value ensures numerical stability of gradients when
                        :math:`n < 1`.
   :type min_response: float, default=1e-10

   .. rubric:: Notes

   Compressive encoding with `gain` :math:`g` and :math:`n` is done according to the equation [1]_:

   .. math::

       p(x) = g \times \text{max}(f(x), \epsilon)^n

   .. rubric:: References

   .. [1] Kay, K. N., Winawer, J., Mezer, A., & Wandell, B. A. (2013). Compressive spatial summation in human visual
       cortex. *Journal of Neurophysiology*, 110(2), 481-494. https://doi.org/10.1152/jn.00105.2013

   .. rubric:: Examples

   Predict an encode population receptive field model response for multiple units.

   >>> import numpy as np
   >>> import pandas as pd
   >>> from prfmodel.examples import load_2d_prf_bar_stimulus
   >>> from prfmodel.models.prf import PRFStimulusEncoder
   >>> stimulus = load_2d_prf_bar_stimulus()
   >>> # Create dummy response as input for encdoder
   >>> prf_response = np.ones((3, 128, 128))  # Must have same number of frames as stimulus
   >>> model = CompressiveEncoder(
   ...     encoding_model=PRFStimulusEncoder(),
   ... )
   >>> # Define model parameters for 3 units
   >>> params = pd.DataFrame({
   ...     # Compressive parameters
   ...     "gain": [0.5, 0.1, 1.2],
   ...     "n": [0.4, 0.5, 0.9],
   ... })
   >>> # Predict model response
   >>> resp = model(stimulus, prf_response, params)
   >>> print(resp.shape)  # (num_units, num_frames)
   (3, 170)


   .. py:method:: __call__(stimulus: S, response: prfmodel.typing.Tensor | numpy.ndarray, parameters: pandas.DataFrame, dtype: str | None = None) -> numpy.ndarray

      Encode a model response with a stimulus.

      This is the public entry point; subclasses implement :meth:`call` instead. Use :meth:`call` when a
      backend tensor is required.

      :param stimulus: Stimulus object.
      :type stimulus: Stimulus
      :param response: Model response.
      :type response: :data:`prfmodel.typing.Tensor` or numpy.ndarray
      :param parameters: Dataframe with columns containing different model parameters and rows containing parameter values
                         for different units.
      :type parameters: pandas.DataFrame
      :param dtype: The dtype of the prediction result. If `None` (the default), uses the dtype from
                    :func:`prfmodel.utils.get_dtype`.
      :type dtype: str, optional

      :returns: The stimulus encoded model response with shape `(num_units, ...)` dtype `dtype`. The number of units is
                the number of rows in :attr:`parameters`. The number and size of other axes depends on the stimulus and the
                response.
      :rtype: numpy.ndarray

      :raises ValueError: If `parameters` is missing one or more of :attr:`parameter_names`.



   .. py:method:: call(stimulus: prfmodel.models.base.T, response: prfmodel.typing.Tensor, parameters: prfmodel.utils.TensorFrame) -> prfmodel.typing.Tensor

      Compress and encode a model response with a stimulus.

      Encodes the model response, then compresses and amplifies the encoded response.

      :param stimulus: The tensor-holding stimulus object, from :meth:`~prfmodel.stimuli.Stimulus.to_tensors`.
      :type stimulus: StimulusTensors
      :param response: Model response.
      :type response: Tensor
      :param parameters: Model parameters as tensors, supporting the same column selection as a :class:`pandas.DataFrame`.
      :type parameters: TensorFrame

      :returns: The compressed and stimulus encoded model response with shape `(num_units, ...)`.
                The number of units is the number of rows in `parameters`. The number and size of other axes depends on
                the stimulus and the response.
      :rtype: :data:`prfmodel.typing.Tensor`



   .. py:method:: check_parameter_names(parameters: pandas.DataFrame) -> None

      Check that required parameter names are supplied.

      :param parameters: Dataframe with columns containing different model parameters and rows containing parameter values
                         for different units.
      :type parameters: pandas.DataFrame

      :raises ValueError: When a parameter name in the :attr:`parameter_names` attribute is not a column in ``parameters``.



   .. py:method:: check_parameter_values(parameters: pandas.DataFrame) -> None

      Check that the parameter values lie inside the domain the model is defined on.

      :param parameters: Dataframe with columns containing different model parameters and rows containing parameter values
                         for different units.
      :type parameters: pandas.DataFrame

      :raises ValueError: When a parameter that must be ``> 0`` is zero or negative.



   .. py:method:: get_consumed_parameter_names(parameters: pandas.DataFrame) -> list[str]

      Return the parameter names this model and its submodels read from ``parameters``.

      :param parameters: Dataframe with columns containing different model parameters and rows containing parameter values
                         for different units.
      :type parameters: pandas.DataFrame

      :returns: Names of the parameters this model and its submodels read from ``parameters``.
      :rtype: list of str



   .. py:attribute:: models

      A dictionary with the named submodels.

      :param models: Named submodels.
      :type models: dict of ModelProtocol

      :raises TypeError: When a submodel does not inherit from :class:`ModelProtocol`.


   .. py:property:: parameter_names
      :type: list[str]


      A list with names of unique parameters that are used by the submodels.

